import { Tale } from "@/types/tale";
import { musicienBremeContent } from "./content";

export const musiciensBreme: Tale = {
  title: "Les musiciens de Brême",
  content: [
    ...musicienBremeContent
  ],
  moral: "L'union fait la force. Même quand on est rejeté par les autres, on peut trouver sa place en s'unissant avec des amis qui partagent le même destin."
};